;; -*- mode: emacs-lisp ; coding: utf-8 -*-

(el-get 'sync '(multi-shell))

(require 'multi-shell)

(setq multi-shell-revert-window-after-complete nil)
(setq multi-shell-revert-window-keystroke (kbd "q"))
